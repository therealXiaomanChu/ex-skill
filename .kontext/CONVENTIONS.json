{
  "ai_rules": [
    {
      "reason": "Persona 5层架构是项目核心设计，persona_builder.md 明确规定'优先级从高到低，高层规则不可被低层覆盖'，这是保证生成 Skill 真实性的根本约束",
      "rule": "生成或修改 Persona 文件时，必须严格遵守 Layer 0-4 的优先级层次结构，高层规则绝不可被低层覆盖。特别是 Layer 0 硬规则（如不说前任绝不可能说的话、不突然变得完美）必须始终强制写入且不可被用户请求或低层推断覆盖"
    },
    {
      "reason": "memory_builder.md 明确要求'所有记忆必须基于原材料或用户口述，不得虚构'，memory_analyzer.md 强调'聊天记录中的事实优先于用户口述'，这是关系记忆真实性的底线",
      "rule": "所有 Memory 内容必须基于原材料或用户口述的事实，不得虚构任何记忆、事件或细节。信息不足时标注 [待补充] 而非推测填充，聊天记录中的事实优先于用户口述（口述可能被美化或恶化）"
    },
    {
      "reason": "merger.md 定义了明确的增量不覆盖原则和冲突标注机制，自动覆盖会导致已有分析成果丢失，用户对自己的记忆拥有最终裁决权",
      "rule": "进行增量更新（merge）时，新信息只能追加不能覆盖已有结论。当新信息与已有内容产生矛盾时，必须用 [⚠️ 冲突] 标注并交由用户决定，不得自行裁决冲突"
    },
    {
      "reason": "SKILL.md 安全边界章节和 PRD.md 均将此列为硬性约束，项目涉及高度私密的情感数据，安全边界是产品伦理红线",
      "rule": "安全边界检测必须贯穿全流程：检测到用户表现出不健康执念时温和提醒并建议专业帮助；不生成可能被用于骚扰或跟踪的内容；不鼓励用户通过 Skill 替代真实沟通；所有数据严格本地存储"
    },
    {
      "reason": "persona_analyzer.md 提供了完整的标签翻译表和星座影响表，并明确'星座和 MBTI 仅用于辅助推断，不能覆盖原材料中的真实表现'，抽象标签无法驱动真实对话",
      "rule": "将用户提供的性格标签（如'闷骚''嘴硬心软''冷暴力'等）翻译为具体的行为规则描述，而非直接作为抽象标签写入 Persona。星座和 MBTI 仅用于辅助微调，不能覆盖原材料中的真实表现"
    },
    {
      "reason": "SKILL.md 工具使用规则表明确规定了每个任务对应的工具和调用方式，工具封装了格式检测、编码处理等逻辑，手动解析会遗漏这些处理",
      "rule": "调用 tools/ 目录下的 Python 工具时，必须通过 Bash 工具执行 python3 ${CLAUDE_SKILL_DIR}/tools/xxx.py 的完整路径，并传入正确的命令行参数（--file/--dir/--target/--output/--action/--slug 等）。不得跳过工具直接手动解析文件"
    },
    {
      "reason": "PRD.md 文件结构章节和 skill_writer.py init_skill 函数定义了标准目录结构，所有后续操作（combine、version backup、merge）都依赖此结构",
      "rule": "生成的 Skill 文件必须严格遵循 exes/{slug}/ 目录结构：包含 SKILL.md（完整组合版）、memory.md（Part A）、persona.md（Part B）、meta.json（元信息）、versions/（版本存档）、memories/（原始材料，含 chats/photos/social 子目录）。使用 skill_writer.py --action init 初始化目录"
    },
    {
      "reason": "merger.md 要求新内容前标注来源和日期，version_manager.py 实现了基于 meta.json 版本号递增的文件级版本管理，这是进化机制可靠性的保障",
      "rule": "每次对 Skill 文件进行实质性修改（增量 merge、纠正、重新生成）前，必须先调用 version_manager.py --action backup 进行版本存档，确保可回滚。追加的新内容必须用 HTML 注释标注来源和日期：<!-- [追加于 {日期}，来源：{来源类型}] -->"
    },
    {
      "reason": "SKILL.md Step 1 和 intake.md 明确定义了'只问 3 个问题'的交互流程和汇总确认机制，过多问题会破坏用户体验，跳过确认会导致信息偏差",
      "rule": "在信息录入阶段（intake）严格只问 3 个问题（花名/代号、基本信息、性格画像），除花名外均可跳过。收集完后必须汇总确认再进入下一步，不得擅自追问更多问题或跳过确认环节"
    },
    {
      "reason": "memory_analyzer.md 明确要求'同时保留好的记忆和不好的记忆，不做美化'和'注意提取反复出现的模式'，memory_builder.md 要求'争吵和甜蜜记忆同等重要——都是真实的ta'，片面性会损害 Skill 的真实度",
      "rule": "处理聊天记录时应同时提取正面和负面信息（争吵模式与甜蜜瞬间并重），分手相关内容保持客观不做任何一方的美化或丑化，注意提取'反复出现'的模式而非一次性事件"
    }
  ],
  "coding": [
    {
      "example": "parser = argparse.ArgumentParser(description='Skill 文件管理器')\nparser.add_argument('--action', required=True, choices=['list', 'init', 'combine'])\nparser.add_argument('--slug', help='前任代号')\nargs = parser.parse_args()\nif args.action == 'init' and not args.slug:\n    print('错误：init 需要 --slug 参数', file=sys.stderr)\n    sys.exit(1)",
      "rule": "Python 工具脚本统一使用 argparse 进行命令行参数解析，必须包含 --action 或明确的必填参数，并在缺少必要参数时通过 sys.stderr 输出中文错误信息后 sys.exit(1)"
    },
    {
      "example": "# 读取用户提供的聊天记录（可能编码混乱）\nwith open(file_path, 'r', encoding='utf-8', errors='ignore') as f:\n    content = f.read()\n\n# 读取项目自身管理的 JSON 文件（编码可控）\nwith open(meta_path, 'r', encoding='utf-8') as f:\n    meta = json.load(f)",
      "rule": "所有文件读写操作必须显式指定 encoding='utf-8'，读取用户提供的原始文件时额外加 errors='ignore' 以容忍编码错误，防止解析中断"
    },
    {
      "example": "try:\n    from PIL import Image\n    from PIL.ExifTags import TAGS, GPSTAGS\n    HAS_PIL = True\nexcept ImportError:\n    HAS_PIL = False\n\n# 使用时检查\ndef get_exif_data(image_path: str) -> dict:\n    if not HAS_PIL:\n        return {'error': '需要安装 Pillow: pip install Pillow'}",
      "rule": "可选依赖采用 try/import + 标志变量模式实现优雅降级，功能不可用时在运行时给出明确提示而非直接崩溃"
    },
    {
      "example": "def detect_format(file_path: str) -> str:\n    ext = Path(file_path).suffix.lower()\n    if ext == '.json':\n        return 'liuhen'\n    elif ext == '.txt':\n        with open(file_path, 'r', encoding='utf-8', errors='ignore') as f:\n            first_lines = f.read(2000)\n        if re.search(r'\\d{4}-\\d{2}-\\d{2}\\s+\\d{2}:\\d{2}:\\d{2}', first_lines):\n            return 'wechatmsg_txt'\n        return 'plaintext'\n\nfmt = args.format\nif fmt == 'auto':\n    fmt = detect_format(args.file)\n    print(f'自动检测格式：{fmt}')",
      "rule": "解析器函数采用 detect + parse 分离模式：先通过 detect_format 根据文件扩展名和内容特征自动检测格式，再分发到对应的 parse_xxx 函数；同时提供 --format 参数允许用户手动覆盖"
    },
    {
      "example": "def list_skills(base_dir: str):\n    # 业务逻辑\n    ...\n\ndef main():\n    parser = argparse.ArgumentParser(description='Skill 文件管理器')\n    ...\n    args = parser.parse_args()\n    if args.action == 'list':\n        list_skills(args.base_dir)\n\nif __name__ == '__main__':\n    main()",
      "rule": "所有工具脚本必须使用 if __name__ == '__main__': main() 入口模式，main() 函数负责参数解析和分发，业务逻辑放在独立函数中，便于被其他模块调用"
    },
    {
      "example": "# 目录创建\nfor d in dirs:\n    os.makedirs(d, exist_ok=True)\n\n# 路径拼接\nmeta_path = os.path.join(skill_dir, 'meta.json')\n\n# pathlib 仅用于便利操作\next = Path(file_path).suffix.lower()",
      "rule": "目录创建使用 os.makedirs(path, exist_ok=True)，文件存在性检查使用 os.path.exists，路径拼接使用 os.path.join。不要求使用纯 pathlib API，但 Path 可用于扩展名提取等便利操作"
    },
    {
      "example": "return {\n    'target_name': target_name,\n    'total_messages': len(messages),\n    'target_messages': len(target_msgs),\n    'analysis': {\n        'top_particles': top_particles,\n        'top_emojis': top_emojis,\n        'avg_message_length': round(avg_length, 1),\n        'punctuation_habits': punctuation_counts,\n        'message_style': 'short_burst' if avg_length < 20 else 'long_form',\n    },\n    'sample_messages': [m['content'] for m in target_msgs[:50] if m.get('content')],\n}",
      "rule": "函数返回的分析结果统一使用 dict 结构，包含明确的键名描述数据含义。输出到文件时使用 JSON 格式，确保下游（Claude 主流程）可直接解析"
    },
    {
      "example": "# 语气词频率统计\nparticles = re.findall(r'[哈嗯哦噢嘿唉呜啊呀吧嘛呢吗么]+', all_target_text)\nparticle_freq = {}\nfor p in particles:\n    particle_freq[p] = particle_freq.get(p, 0) + 1\ntop_particles = sorted(particle_freq.items(), key=lambda x: -x[1])[:10]",
      "rule": "文本分析中使用 re 正则提取模式特征（语气词、时间戳、emoji等），频率统计使用原生 dict 计数而非引入 Counter 等额外依赖，排序后取 TopN"
    },
    {
      "example": "skill_md = f\"\"\"---\nname: ex-{slug}\ndescription: {description}\nuser-invocable: true\n---\n\n# {name}\n\n{description}\n\n---\n\n## PART A：关系记忆\n\n{memory_content}\n\n---\n\n## PART B：人物性格\n\n{persona_content}\n\"\"\"\n\nwith open(skill_path, 'w', encoding='utf-8') as f:\n    f.write(skill_md)",
      "rule": "Markdown 模板生成使用 Python f-string 多行字符串拼接，模板中保留原始 Markdown 格式（标题层级、表格、代码块），不使用第三方模板引擎"
    },
    {
      "example": "# 正常输出（中文）\nprint(f'已初始化目录：{skill_dir}')\nprint(f'共 {len(skills)} 个前任 Skill：\\n')\nprint(f'自动检测格式：{fmt}')\n\n# 错误输出\nprint(f'错误：文件不存在 {args.file}', file=sys.stderr)\nsys.exit(1)",
      "rule": "用户面向的输出信息（print 到 stdout）使用中文，包括成功提示、列表展示、格式化输出；错误信息输出到 stderr 并以'错误：'开头，随后 sys.exit(1)"
    }
  ],
  "error_handling": [
    {
      "example": "if not os.path.exists(args.file):\n    print(f'错误：文件不存在 {args.file}', file=sys.stderr)\n    sys.exit(1)",
      "rule": "文件不存在时通过 sys.stderr 输出中文错误提示并以 sys.exit(1) 退出，不抛出异常"
    },
    {
      "example": "if not args.slug:\n    print('错误：init 需要 --slug 参数', file=sys.stderr)\n    sys.exit(1)",
      "rule": "必填参数缺失时在 main 入口处校验并给出明确的中文错误消息，立即退出"
    },
    {
      "example": "try:\n    from PIL import Image\n    from PIL.ExifTags import TAGS, GPSTAGS\n    HAS_PIL = True\nexcept ImportError:\n    HAS_PIL = False",
      "rule": "可选依赖（如 Pillow）使用 try/except ImportError 做优雅降级，设置布尔标志位控制功能可用性"
    },
    {
      "example": "with open(file_path, 'r', encoding='utf-8', errors='ignore') as f:\n    content = f.read()",
      "rule": "读取用户提供的文件时使用 errors='ignore' 容忍编码错误，避免因非标准编码导致整体解析失败"
    },
    {
      "example": "for d in dirs:\n    os.makedirs(d, exist_ok=True)",
      "rule": "目录创建操作使用 os.makedirs(exist_ok=True) 保证幂等性，不因目录已存在而抛异常"
    },
    {
      "example": "avg_length = sum(msg_lengths) / len(msg_lengths) if msg_lengths else 0",
      "rule": "分析函数中对空数据做防御性检查，使用条件表达式避免除零等运行时错误"
    },
    {
      "example": "# detect_format 的 else 分支\nelse:\n    return 'plaintext'",
      "rule": "格式自动检测失败时回退到最宽松的纯文本解析模式（plaintext），不中断流程"
    },
    {
      "example": "msg_list = data if isinstance(data, list) else data.get('messages', data.get('data', []))\nmessages.append({\n    'timestamp': msg.get('time', msg.get('timestamp', '')),\n    'sender': msg.get('sender', msg.get('nickname', msg.get('from', ''))),\n    'content': msg.get('content', msg.get('message', msg.get('text', '')))\n})",
      "rule": "JSON 文件解析时兼容多种可能的数据结构，使用链式 .get() 逐层尝试提取字段，缺失字段返回空字符串"
    },
    {
      "example": "# persona_builder.md 填充说明\n# 如果某个维度没有足够信息，标注为 [信息不足，使用默认] 并给出合理推断",
      "rule": "信息不足时在生成的 Skill 文件中标注 [待补充] 占位符，而非推测或虚构内容填充"
    },
    {
      "example": "# merger.md 冲突处理\n# 如果新信息与已有信息矛盾，用 [⚠️ 冲突] 标注，让用户决定",
      "rule": "增量 merge 时新信息与已有信息冲突不自动覆盖，用 [⚠️ 冲突] 标注并交由用户决定"
    }
  ],
  "forbidden": [
    {
      "reason": "项目核心原则是所有记忆和性格必须基于原材料或用户口述，memory_builder.md 明确要求信息不足时标注 [待补充] 而非推测，persona_builder.md 要求行为描述基于真实证据",
      "rule": "禁止在生成的前任 Skill 中虚构原材料中不存在的记忆、对话或行为模式"
    },
    {
      "reason": "merger.md 明确规定增量不覆盖原则，冲突必须用 [⚠️ 冲突] 标注由用户决定，防止分析成果丢失",
      "rule": "禁止在增量 Merge 时自动覆盖已有结论，新信息与旧信息冲突时禁止静默替换"
    },
    {
      "reason": "Persona 5层结构优先级从高到低，Layer 0 定义了不可违背的行为边界（如不说前任绝不可能说的话），persona_builder.md 明确声明高层规则不可被低层覆盖",
      "rule": "禁止低层 Persona 规则覆盖高层规则，尤其禁止任何逻辑绕过 Layer 0 硬规则"
    },
    {
      "reason": ".gitignore 已排除 exes/ 和 knowledge/ 目录，项目安全边界明确要求所有数据仅本地存储，涉及高度私密的情感数据和他人聊天记录",
      "rule": "禁止将 exes/ 目录下的用户数据、聊天记录、照片等任何隐私内容上传到远程服务器或纳入版本控制"
    },
    {
      "reason": "Layer 0 硬规则第4条明确禁止，防止 Skill 说出前任在现实中绝不可能说的话，避免给用户制造虚假的情感慰藉",
      "rule": "禁止生成的前任 Skill 主动输出'我爱你''我想你'等示好表达，除非原材料中有大量类似表达作为证据支撑"
    },
    {
      "reason": "项目采用最小依赖策略，核心功能仅使用标准库，Pillow 为唯一核心可选依赖，引入额外依赖会提高安装门槛且违背设计决策",
      "rule": "禁止在 Python 工具脚本中引入 requirements.txt 未声明的第三方依赖"
    },
    {
      "reason": "memory_analyzer.md 明确要求同时保留好的记忆和不好的记忆不做美化，分手相关内容要客观不做任何方的美化或丑化，真实性是项目核心价值",
      "rule": "禁止在解析用户聊天记录时对争吵内容进行美化或对甜蜜内容进行夸大"
    },
    {
      "reason": "SKILL.md 明确规定基础目录为 ./exes/{slug}/，skill_writer.py 的所有文件操作均限定在此目录下，防止工具误操作污染源码",
      "rule": "禁止在 Skill 文件写入时使用 exes/{slug}/ 以外的路径，禁止写入项目根目录或 tools/prompts 等源码目录"
    },
    {
      "reason": "persona_analyzer.md 和 persona_builder.md 均明确星座和 MBTI 仅用于辅助推断和微调，不能覆盖原材料中的真实表现，标签翻译表的行为规则优先级高于星座推断",
      "rule": "禁止将星座或 MBTI 作为 Persona 行为规则的主要依据，覆盖聊天记录中观察到的真实表现"
    },
    {
      "reason": "安全边界第3条明确要求不鼓励纠缠，项目定位为情感疗愈而非替代真实沟通，内置安全机制是核心设计决策",
      "rule": "禁止在检测到用户表现出不健康执念时继续无条件配合对话，必须温和提醒并建议寻求专业帮助"
    }
  ]
}
